version: '3.8'

services:
  bot:
    build: .
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///data/data.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
